<?php
function p5b3GuardSnapshot(array $ctx):array{$rows=['reservation'=>dbFetchOne('SELECT status,vehicle_id,customer_id FROM reservations WHERE id=:id',['id'=>$ctx['reservation']]),'contract'=>dbFetchOne('SELECT status,current_version,current_version_id,issued_at,signed_at,activated_at,completed_at,cancelled_at FROM rental_contracts WHERE id=:id',['id'=>$ctx['contract']]),'vehicle'=>dbFetchOne('SELECT status,current_mileage,archived_at FROM vehicles WHERE id=:id',['id'=>$ctx['vehicle']]),'inspection'=>dbFetchOne('SELECT status,completed_at,archived_at,reservation_id,contract_id,vehicle_id,customer_id,inspection_type FROM vehicle_inspections WHERE id=:id',['id'=>$ctx['inspection']]),'photos'=>dbFetchAll('SELECT id,photo_slot,storage_path,mime_type,file_size,sha256,archived_at FROM inspection_photos WHERE inspection_id=:id ORDER BY id',['id'=>$ctx['inspection']])];$rows['files']=[];foreach($rows['photos']as$photo){$path=inspectionPhotoPath((string)$photo['storage_path']);$rows['files'][$photo['id']]=$path&&is_file($path)?hash_file('sha256',$path):null;}$rows['reservation_history']=(int)dbFetchOne("SELECT COUNT(*) n FROM reservation_status_history WHERE reservation_id=:id AND from_status='ready' AND to_status='active'",['id'=>$ctx['reservation']])['n'];$rows['contract_history']=(int)dbFetchOne("SELECT COUNT(*) n FROM contract_status_history WHERE contract_id=:id AND from_status='signed' AND to_status='active'",['id'=>$ctx['contract']])['n'];$rows['audit']=(int)dbFetchOne("SELECT COUNT(*) n FROM audit_logs WHERE entity_id=:id AND action='rental.checkout'",['id'=>$ctx['reservation']])['n'];return$rows;}
function p5b3Guard(array $fixture,string $label,callable $mutate,array &$files,callable $assert):void{$ctx=p5b3Eligible($fixture,'G'.substr(md5($label),0,7),$files);$command=$ctx['command'];$changed=$mutate($ctx,$command);if(is_array($changed))$command=$changed;$before=p5b3GuardSnapshot($ctx);$refused=false;try{rentalCheckout($command);}catch(Throwable){$refused=true;}$after=p5b3GuardSnapshot($ctx);$assert($refused,$label.' was accepted');$assert($after===$before,$label.' changed business, history, audit, photo, or file state');$assert((int)dbFetchOne("SELECT COUNT(*) n FROM rental_operation_idempotency_keys WHERE origin_agency_id=:agency AND operation_type='rental_checkout' AND status='in_progress'",['agency'=>$fixture['agency_id']])['n']===0,$label.' left in-progress idempotency');dbExecute("UPDATE reservations SET status='cancelled' WHERE id=:id",['id'=>$ctx['reservation']]);}
function p5b3RunGuardMatrix(array $fixture,array $foreign,array &$files,callable $assert):array{$labels=[];$case=function(string $label,callable $mutate)use($fixture,&$files,$assert,&$labels):void{phase5bAs($fixture,ROLE_AGENCY_MANAGER);p5b3Guard($fixture,$label,$mutate,$files,$assert);$labels[]=$label;};$command=static fn(string $field,$value):callable=>static function(array $ctx,array $input)use($field,$value):array{$input[$field]=$value;return$input;};
    $case('contract nonexistent',$command('contract_id',999999999));$other=p5b3Eligible($fixture,'GOTHER',$files);$case('contract belongs another reservation',static function(array $ctx,array $input)use($other):array{$input['contract_id']=$other['contract'];return$input;});dbExecute("UPDATE reservations SET status='cancelled' WHERE id=:id",['id'=>$other['reservation']]);
    $case('contract draft',static function(array $ctx,array $input):void{dbExecute("UPDATE rental_contracts SET status='draft',issued_at=NULL,signed_at=NULL,activated_at=NULL,completed_at=NULL WHERE id=:id",['id'=>$ctx['contract']]);});$case('contract issued unsigned',static function(array $ctx,array $input):void{dbExecute("UPDATE rental_contracts SET status='issued',signed_at=NULL,activated_at=NULL,completed_at=NULL WHERE id=:id",['id'=>$ctx['contract']]);});$case('contract cancelled',static function(array $ctx,array $input):void{dbExecute("UPDATE rental_contracts SET status='cancelled',cancelled_at=NOW(6),cancelled_by=:actor,cancellation_reason='guard' WHERE id=:id",['actor'=>currentUserId(),'id'=>$ctx['contract']]);});$case('contract active',static function(array $ctx,array $input):void{dbExecute("UPDATE rental_contracts SET status='active',activated_at=NOW(6) WHERE id=:id",['id'=>$ctx['contract']]);});$case('only customer acknowledgement',static function(array $ctx,array $input):void{dbExecute("DELETE FROM contract_acknowledgements WHERE contract_id=:id AND acknowledgement_type='agency_representative'",['id'=>$ctx['contract']]);});$case('only representative acknowledgement',static function(array $ctx,array $input):void{dbExecute("DELETE FROM contract_acknowledgements WHERE contract_id=:id AND acknowledgement_type='customer'",['id'=>$ctx['contract']]);});$case('acknowledgement obsolete version',static function(array $ctx,array $input):void{$old=dbFetchOne('SELECT * FROM contract_versions WHERE id=(SELECT current_version_id FROM rental_contracts WHERE id=:id)',['id'=>$ctx['contract']]);dbExecute('INSERT INTO contract_versions(agency_id,contract_id,version_number,language_code,predecessor_version_id,snapshot_json,snapshot_sha256,terms_text,change_reason,created_by)VALUES(:agency,:contract,2,\'en\',:old,:snapshot,:sha,:terms,\'guard\',:actor)',['agency'=>$old['agency_id'],'contract'=>$ctx['contract'],'old'=>$old['id'],'snapshot'=>$old['snapshot_json'],'sha'=>$old['snapshot_sha256'],'terms'=>$old['terms_text'],'actor'=>currentUserId()]);dbExecute('UPDATE rental_contracts SET current_version=2,current_version_id=:version WHERE id=:id',['version'=>db()->lastInsertId(),'id'=>$ctx['contract']]);});dbExecute('UPDATE rental_contracts rc JOIN contract_versions cv ON cv.id=rc.current_version_id SET rc.current_version_id=cv.predecessor_version_id,rc.current_version=1 WHERE rc.agency_id=:agency AND cv.version_number=2',['agency'=>$fixture['agency_id']]);dbExecute('DELETE FROM contract_versions WHERE agency_id=:agency AND version_number=2',['agency'=>$fixture['agency_id']]);$case('current version missing',static function(array $ctx,array $input):void{dbExecute('UPDATE rental_contracts SET current_version_id=NULL WHERE id=:id',['id'=>$ctx['contract']]);});
    $case('reservation nonexistent',$command('reservation_id',999999999));foreach(['cancelled','completed','active']as$status)$case('reservation '.$status,static function(array $ctx,array $input)use($status):void{dbExecute('UPDATE reservations SET status=:status WHERE id=:id',['status'=>$status,'id'=>$ctx['reservation']]);});$case('reservation without vehicle',static function(array $ctx,array $input):void{dbExecute('UPDATE reservations SET vehicle_id=NULL WHERE id=:id',['id'=>$ctx['reservation']]);});$case('reservation blocked customer',static function(array $ctx,array $input):void{dbExecute("UPDATE customers SET status='blocked' WHERE id=(SELECT customer_id FROM reservations WHERE id=:id)",['id'=>$ctx['reservation']]);});dbExecute("UPDATE customers SET status='regular' WHERE id=:id",['id'=>$fixture['customer_id']]);
    foreach(['available','rented','maintenance']as$status)$case('vehicle '.$status,static function(array $ctx,array $input)use($status):void{dbExecute('UPDATE vehicles SET status=:status WHERE id=:id',['status'=>$status,'id'=>$ctx['vehicle']]);});$case('vehicle archived',static function(array $ctx,array $input):void{dbExecute('UPDATE vehicles SET archived_at=NOW(6) WHERE id=:id',['id'=>$ctx['vehicle']]);});dbExecute('UPDATE vehicles SET archived_at=NULL WHERE id=:id',['id'=>$fixture['vehicle_id']]);$case('vehicle mileage above inspection',static function(array $ctx,array $input):void{dbExecute('UPDATE vehicles SET current_mileage=126 WHERE id=:id',['id'=>$ctx['vehicle']]);});
    $case('inspection nonexistent',$command('inspection_id',999999999));$case('inspection return type',static function(array $ctx,array $input):void{dbExecute("UPDATE vehicle_inspections SET inspection_type='return' WHERE id=:id",['id'=>$ctx['inspection']]);});$alternate=p5b3Eligible($fixture,'ALTRES',$files);$case('inspection wrong reservation',static function(array $ctx,array $input)use($alternate):void{dbExecute('UPDATE vehicle_inspections SET reservation_id=:value WHERE id=:id',['value'=>$alternate['reservation'],'id'=>$ctx['inspection']]);});dbExecute("UPDATE reservations SET status='cancelled' WHERE id=:id",['id'=>$alternate['reservation']]);$case('inspection wrong contract',static function(array $ctx,array $input)use($other):array{$input['contract_id']=$other['contract'];return$input;});dbExecute("INSERT INTO vehicles(agency_id,category_id,registration_number,vin,brand,model,base_daily_price,recommended_deposit,status,created_by,updated_by) SELECT agency_id,category_id,:reg,:vin,brand,model,base_daily_price,recommended_deposit,'reserved',created_by,updated_by FROM vehicles WHERE id=:id",['reg'=>'G'.bin2hex(random_bytes(6)),'vin'=>'GV'.bin2hex(random_bytes(12)),'id'=>$fixture['vehicle_id']]);$extraVehicle=(int)db()->lastInsertId();$case('inspection wrong vehicle',static function(array $ctx,array $input)use($extraVehicle):void{dbExecute('UPDATE vehicle_inspections SET vehicle_id=:value WHERE id=:id',['value'=>$extraVehicle,'id'=>$ctx['inspection']]);});dbExecute("INSERT INTO customers(agency_id,first_name,last_name,identity_number,licence_number,status,created_by,updated_by)VALUES(:agency,'Guard','Other',:identity,:licence,'regular',:actor,:actor2)",['agency'=>$fixture['agency_id'],'identity'=>'GI'.bin2hex(random_bytes(5)),'licence'=>'GL'.bin2hex(random_bytes(5)),'actor'=>currentUserId(),'actor2'=>currentUserId()]);$extraCustomer=(int)db()->lastInsertId();$case('inspection wrong customer',static function(array $ctx,array $input)use($extraCustomer):void{dbExecute('UPDATE vehicle_inspections SET customer_id=:value WHERE id=:id',['value'=>$extraCustomer,'id'=>$ctx['inspection']]);});$case('inspection completed',static function(array $ctx,array $input):void{dbExecute("UPDATE vehicle_inspections SET status='completed',completed_at=NOW(6) WHERE id=:id",['id'=>$ctx['inspection']]);});$case('inspection archived',static function(array $ctx,array $input):void{dbExecute("UPDATE vehicle_inspections SET status='archived',archived_at=NOW(6) WHERE id=:id",['id'=>$ctx['inspection']]);});
    foreach(inspectionPhotoSlots()as$slot)$case('photo missing '.$slot,static function(array $ctx,array $input)use($slot):void{dbExecute('DELETE FROM inspection_photos WHERE inspection_id=:id AND photo_slot=:slot',['id'=>$ctx['inspection'],'slot'=>$slot]);});$case('photo archived required slot',static function(array $ctx,array $input):void{dbExecute("UPDATE inspection_photos SET archived_at=NOW(6),archived_by=:actor,archive_reason='guard' WHERE inspection_id=:id AND photo_slot='front'",['actor'=>currentUserId(),'id'=>$ctx['inspection']]);});foreach(['storage_path'=>'invalid/path.png','mime_type'=>'image/jpeg','file_size'=>1,'sha256'=>str_repeat('0',64)]as$field=>$value)$case('photo invalid '.$field,static function(array $ctx,array $input)use($field,$value):void{dbExecute("UPDATE inspection_photos SET $field=:value WHERE inspection_id=:id AND photo_slot='front'",['value'=>$value,'id'=>$ctx['inspection']]);});$case('photo file missing',static function(array $ctx,array $input):void{$photo=dbFetchOne("SELECT storage_path FROM inspection_photos WHERE inspection_id=:id AND photo_slot='front'",['id'=>$ctx['inspection']]);$path=inspectionPhotoPath($photo['storage_path']);if($path&&is_file($path))unlink($path);});
    $schemaCtx=p5b3Eligible($fixture,'PHOTOSCHEMA',$files);$duplicateRefused=false;try{dbExecute("INSERT INTO inspection_photos(agency_id,inspection_id,photo_type,photo_slot,storage_path,original_name,mime_type,file_size,sha256,protected_file,captured_at,created_by) SELECT agency_id,inspection_id,photo_type,photo_slot,CONCAT(storage_path,'.duplicate'),original_name,mime_type,file_size,sha256,protected_file,NOW(6),created_by FROM inspection_photos WHERE inspection_id=:id AND photo_slot='front'",['id'=>$schemaCtx['inspection']]);}catch(Throwable){$duplicateRefused=true;}$unexpectedRefused=false;try{dbExecute("UPDATE inspection_photos SET photo_slot='unexpected' WHERE inspection_id=:id AND photo_slot='front'",['id'=>$schemaCtx['inspection']]);}catch(Throwable){$unexpectedRefused=true;}$assert($duplicateRefused,'Schema accepted duplicate/seventh active photo slot');$assert($unexpectedRefused,'Schema accepted unexpected photo slot');$labels[]='duplicate/seventh active slot schema refusal';$labels[]='unexpected slot schema refusal';dbExecute("UPDATE reservations SET status='cancelled' WHERE id=:id",['id'=>$schemaCtx['reservation']]);
    foreach([['mileage','-1'],['mileage','abc'],['mileage','1.5'],['mileage','124'],['mileage','126'],['fuel_level','-1'],['fuel_level','101'],['fuel_level','abc'],['fuel_level','50.001'],['fuel_level','49.00'],['handed_over_at','bad'],['handed_over_at',(new DateTimeImmutable('now'))->modify('+6 minutes')->format('Y-m-d H:i:s')],['handed_over_at',(new DateTimeImmutable('now'))->modify('-2 days')->format('Y-m-d H:i:s')],['handed_over_at',(new DateTimeImmutable('now'))->modify('+2 days')->format('Y-m-d H:i:s')],['comment',str_repeat('x',1001)]]as[$field,$value])$case('value '.$field.' '.substr(md5((string)$value),0,5),$command($field,$value));
    foreach([[ROLE_ACCOUNTANT,'accountant'],[ROLE_FLEET_AGENT,'fleet agent'],[ROLE_CUSTOMER,'customer']]as[$role,$name])$case('actor '.$name,static function(array $ctx,array $input)use($fixture,$role):array{$_SESSION=['user_id'=>$fixture['users'][$role]??$fixture['users'][ROLE_ACCOUNTANT],'role'=>$role,'agency_ids'=>[$fixture['agency_id']],'_authenticated_at'=>time()];return$input;});phase5bAs($fixture,ROLE_AGENCY_MANAGER);$case('inactive agency manager',static function(array $ctx,array $input)use($fixture):array{dbExecute("UPDATE users SET status='inactive' WHERE id=:id",['id'=>$fixture['users'][ROLE_AGENCY_MANAGER]]);return$input;});dbExecute("UPDATE users SET status='active' WHERE id=:id",['id'=>$fixture['users'][ROLE_AGENCY_MANAGER]]);$case('inactive rental agent',static function(array $ctx,array $input)use($fixture):array{dbExecute("UPDATE users SET status='inactive' WHERE id=:id",['id'=>$fixture['users'][ROLE_RENTAL_AGENT]]);$_SESSION=['user_id'=>$fixture['users'][ROLE_RENTAL_AGENT],'role'=>ROLE_RENTAL_AGENT,'agency_ids'=>[$fixture['agency_id']],'_authenticated_at'=>time()];return$input;});dbExecute("UPDATE users SET status='active' WHERE id=:id",['id'=>$fixture['users'][ROLE_RENTAL_AGENT]]);phase5bAs($fixture,ROLE_AGENCY_MANAGER);
    $foreignCtx=p5b3Eligible($foreign,'FOREIGN',$files);phase5bAs($fixture,ROLE_AGENCY_MANAGER);$foreignCommand=$foreignCtx['command'];$before=p5b3GuardSnapshot($foreignCtx);$refused=false;try{rentalCheckout($foreignCommand);}catch(Throwable){$refused=true;}$assert($refused&&p5b3GuardSnapshot($foreignCtx)===$before,'cross-agency checkout was not safely refused');$labels[]='cross-agency contract/reservation/inspection';dbExecute("UPDATE reservations SET status='cancelled' WHERE id=:id",['id'=>$foreignCtx['reservation']]);return$labels;}
