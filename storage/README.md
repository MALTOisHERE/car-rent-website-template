# Private application storage

Uploaded documents and inspection media are stored below this directory with generated names.
The web server must deny direct HTTP access. Files should be delivered only through an authorized PHP controller.
