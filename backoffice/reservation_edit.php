<?php
require_once __DIR__ . '/_layout.php';requirePermission('reservations.manage');
if(requestMethod()==='POST'){requireCsrfPost();try{updateReservationAllocation((int)($_POST['reservation_id']??0),['vehicle_id'=>(int)($_POST['vehicle_id']??0),'pickup_at'=>$_POST['pickup_at']??'','return_at'=>$_POST['return_at']??'']);flash('success','Reservation allocation updated after a concurrency-safe conflict check.');}catch(InvalidArgumentException|DomainException $exception){flash('danger',$exception->getMessage());}catch(Throwable $exception){reportDatabaseError($exception,'Reservation allocation update failed');flash('danger','The allocation could not be updated.');}safeRedirect('reservation_edit.php');}$ids=currentAgencyIds();if(!$ids)$ids=[0];$ph=implode(',',array_fill(0,count($ids),'?'));$reservations=dbFetchAll("SELECT r.*,c.first_name,c.last_name,v.registration_number FROM reservations r JOIN customers c ON c.id=r.customer_id LEFT JOIN vehicles v ON v.id=r.vehicle_id WHERE r.agency_id IN ($ph) AND r.status IN ('draft','quote','pending','confirmed','deposit_paid','ready','active') ORDER BY r.pickup_at",$ids);$vehicles=dbFetchAll("SELECT id,registration_number,brand,model,status FROM vehicles WHERE agency_id IN ($ph) AND archived_at IS NULL AND status NOT IN ('maintenance','damaged','blocked','sold','retired') ORDER BY registration_number",$ids);backofficeHeader('Reservation allocation','reservation_edit.php');?><div class="page-head"><h1>Edit dates, extend, or replace a vehicle</h1></div><section class="card"><p>The server locks the reservation and target vehicle, rechecks overlaps and maintenance, then recalculates totals from the stored agreed daily price or the replacement vehicle price.</p><form method="post"><?=csrfField()?><label>Reservation<select name="reservation_id" id="reservation" required><?php foreach($reservations as $r):?><option value="<?=e($r['id'])?>" data-pickup="<?=e(date('Y-m-d\TH:i',strtotime($r['pickup_at'])))?>" data-return="<?=e(date('Y-m-d\TH:i',strtotime($r['return_at'])))?>" data-vehicle="<?=e($r['vehicle_id'])?>"><?=e($r['reference'].' — '.$r['first_name'].' '.$r['last_name'].' — '.$r['status'])?></option><?php endforeach;?></select></label><label>Vehicle<select name="vehicle_id" id="vehicle"><?php foreach($vehicles as $v):?><option value="<?=e($v['id'])?>"><?=e($v['registration_number'].' '.$v['brand'].' '.$v['model'].' — '.$v['status'])?></option><?php endforeach;?></select></label><label>Pickup<input type="datetime-local" name="pickup_at" id="pickup" required></label><label>Return / extension<input type="datetime-local" name="return_at" id="return" required></label><button class="btn primary">Validate and update allocation</button></form></section><script nonce=""><!--
const reservation=document.getElementById('reservation');function fill(){const o=reservation.options[reservation.selectedIndex];document.getElementById('pickup').value=o?.dataset.pickup||'';document.getElementById('return').value=o?.dataset.return||'';document.getElementById('vehicle').value=o?.dataset.vehicle||'';}reservation.addEventListener('change',fill);fill();
//--></script><?php backofficeFooter();
